#include <iostream>
using namespace std;

int main()
{
    int c, f;
    cin >> f;
    c = (5.0 / 9) * (f - 32);
    cout << c << endl;
    cout << (5.0 / 9) * (f - 32);
    cout << c << endl;
    return 0;
}